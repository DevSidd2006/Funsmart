import { Studio } from './Studio'

// Set the title of the Studio tab in the browser
export const metadata = {
  title: 'FunSmartism Studio',
}

export default function StudioPage() {
  return <Studio />
}
